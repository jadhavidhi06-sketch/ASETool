# ASETool - Required Python Packages
# Install all dependencies using: pip install -r requirements.txt

# Core DNS resolution
dnspython>=2.3.0

# WHOIS lookup
python-whois>=0.8.0

# HTTP requests and header grabbing
requests>=2.31.0
urllib3>=2.0.0

# Progress bars for long operations
tqdm>=4.66.0

# Nmap integration (port scanning)
python-nmap>=0.7.1

# Cryptography (optional, for future features)
cryptography>=41.0.0

# Note: Nmap system package is also required
# Install Nmap from: https://nmap.org/download.html
# Or use your system's package manager:
#   - Ubuntu/Debian: sudo apt install nmap
#   - macOS: brew install nmap
#   - Windows: Download installer from nmap.org